# Frontend Product Design: Netflix

## 📋 Введение

Добро пожаловать на урок по **Product Design** для фронтенд-инженеров. Сегодня мы рассмотрим проектирование Netflix.

### 🔄 Product Design vs System Design

**System Design:**
- Фокус на **одной ключевой фичe** (например, Infinite Scroll, Masonry Layout)
- Глубокое погружение в детали реализации
- Оптимизация конкретного функционала

**Product Design:**
- **Холистический взгляд** на всё приложение
- Поверхностный обзор всех фич
- Архитектура продукта целиком
- Нет "правильного" ответа — важна аргументация

**Особенность Netflix:** Нет одной "звёздной" фичи — нужно охватить весь продукт.

---

## 📝 План Product Design

1. ✅ **Общие требования (General Requirements)**
2. ✅ **Функциональные требования (Functional Requirements)**
3. ✅ **Архитектура приложения и компонентов**
4. ✅ **Модели данных (Data Entities)**
5. ✅ **API данных (Data API)**
6. ✅ **Хранилище данных (Data Store)**
7. ✅ **Оптимизация (Optimization)**
8. ✅ **Доступность (Accessibility)**

---

## 1. 📌 Общие требования

### Основной функционал:

**1. Поиск фильмов:**
- По актёру
- По жанру
- По названию

**2. Просмотр контента:**
- Короткий превью фильма (при hover)
- Полноэкранный просмотр

**3. Навигация:**
- По жанрам
- Рекомендации
- "Мой список"

---

## 2. 🎯 Функциональные требования

### Платформы:

```
✅ Smart TV
✅ Tablets (iPad)
✅ Smartphones
✅ Desktops
```

### Адаптация к сети:

**1. Adapt video to network bandwidth**
- Низкое качество для слабого интернета
- Автоматическое переключение качества
- Минимум — возможность смотреть

**2. Optimize images/promos to viewport and network**
- Адаптивные превью
- GIF вместо видео для промо
- Responsive images

---

## 3. 🏗️ Архитектура приложения

### Design System (обязательно!)

Для большого приложения как Netflix нужна **единая система дизайна**:

```
Design System:
├── Typography (шрифты, размеры)
├── UI Primitives (Button, Select, Input)
├── Grid System (Layout)
└── Common Components (Header, Card, Modal)
```

**Зачем:** Несколько команд работают параллельно — нужна консистентность.

---

### Основные страницы:

```
Netflix Pages:
├── Sign Up ⭐ (критична для конверсии)
├── Main Page ⭐ (основной функционал)
└── Profile Management (не фокусируемся)
```

**Фокус на интервью:**
- Main Page — основная функциональность
- Sign Up — в секции оптимизации

---

### Main Page — Структура

```
┌─────────────────────────────────────────────┐
│              HEADER                         │
│  ┌─────────────┬────────────────────────┐  │
│  │  Genres:    │  Actions:              │  │
│  │  - Latest   │  - Search              │  │
│  │  - My List  │  - Profile Menu        │  │
│  │  - Genre 1  │    └─ Account          │  │
│  │  - Genre 2  │    └─ Help Center      │  │
│  └─────────────┴────────────────────────┘  │
├─────────────────────────────────────────────┤
│         PROMO MOVIE (Hero Section)          │
│  ┌───────────────────────────────────────┐  │
│  │  [Large Preview of Featured Movie]    │  │
│  │                                       │  │
│  │  [▶ Play]  [ℹ More Info]             │  │
│  └───────────────────────────────────────┘  │
├─────────────────────────────────────────────┤
│         MOVIE DASHBOARD                     │
│  ┌─────────────────────────────────────┐   │
│  │  My List                            │   │
│  │  [Card] [Card] [Card] [Card] →      │   │
│  └─────────────────────────────────────┘   │
│  ┌─────────────────────────────────────┐   │
│  │  TV Shows                           │   │
│  │  [Card] [Card] [Card] [Card] →      │   │
│  └─────────────────────────────────────┘   │
│  ┌─────────────────────────────────────┐   │
│  │  Action Movies                      │   │
│  │  [Card] [Card] [Card] [Card] →      │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

### Movie Card — Интерактивность

```
Normal State:
┌──────────┐
│  [Image] │
│          │
│  Title   │
└──────────┘

Hover State:
┌──────────────────┐
│  [GIF Preview]   │ ← Автоплей превью
│                  │
│  [▶] [+] [ℹ]    │ ← Действия
│  Title           │
│  Match: 95%      │
└──────────────────┘

Click "More Info":
┌────────────────────────────────┐
│  [Full Preview]                │
│                                │
│  [▶ Play] [+] [👍] [👎]        │
│                                │
│  Description...                │
│  Genre: Action, Drama          │
│  Cast: Actor1, Actor2          │
│                                │
│  Episodes: (если сериал)       │
│  [Ep1] [Ep2] [Ep3]            │
│                                │
│  More Like This:               │
│  [Card] [Card] [Card]          │
└────────────────────────────────┘
```

---

### Search Page

```
Header (тот же)
   ↓
No Promo Section ❌
   ↓
┌─────────────────────────────────┐
│ Search: "Jackie Chan"           │
├─────────────────────────────────┤
│ Suggested: "Chan", "Action"     │
├─────────────────────────────────┤
│ Results (Grid):                 │
│  [Card] [Card] [Card] [Card]    │
│  [Card] [Card] [Card] [Card]    │
│  [Card] [Card] [Card] [Card]    │
└─────────────────────────────────┘
```

**Ключевое:** Переиспользуем структуру Main Page, убираем Promo.

---

### Component Hierarchy

```
App
└── Router
    ├── SignUp Page
    ├── Main Page ⭐
    │   ├── Header
    │   │   ├── GenresFeature
    │   │   └── HeaderActions
    │   │       ├── Search
    │   │       └── ProfileMenu
    │   └── PageContent
    │       ├── PromoMovie
    │       │   ├── Preview
    │       │   └── Actions
    │       └── MovieDashboard
    │           └── MovieList × N (по тегам)
    │               └── MovieCard × N
    │                   ├── Preview (hover)
    │                   └── MovieCardFull (modal)
    │                       ├── FullPreview
    │                       ├── Details
    │                       ├── Episodes
    │                       └── MoreLikeThis
    └── Profile Page
```

---

## 4. 📊 Модели данных

```typescript
interface Movie {
  id: number;
  title: string;
  previewUrl: string;        // GIF для hover
  description?: string;      // Опционально (lazy load)
  episodes: Episode[];       // 1 для фильма, N для сериала
  rating: number;
  userRating?: number;
  cast: Actor[];
  tags: string[];            // ["Action", "Drama"]
}

interface Episode {
  id: number;
  movieId: number;          // Связь с Movie
  title: string;
  previewUrl: string;
  description: string;
  urls: VideoUrl[];         // Разные качества
  tags: string[];
}

interface VideoUrl {
  quality: '480p' | '720p' | '1080p' | '4K';
  url: string;
  bandwidth: number;        // Требуемая скорость
}

interface Actor {
  id: number;
  name: string;
  fullName: string;
  profileUrl: string;
}

// API Response
interface Dashboard {
  [tag: string]: Movie[];   // "My List": [Movie, Movie, ...]
}
```

---

## 5. 🔌 API данных

```typescript
// 1. Получение дашборда (главная страница)
GET /api/dashboard
Params:
  - apiKey: string
  - userId: number
Response: Dashboard

// 2. Поиск фильмов
GET /api/search
Params:
  - apiKey: string
  - userId: number
  - query: string          // "Jackie Chan"
  - tags?: string[]        // Фильтры по жанрам
  - pageSize?: number
Response: Movie[]

// 3. Полная информация о фильме (lazy)
GET /api/movie/:id/full
Response: Movie (с description, cast, episodes)
```

---

## 6. 🗄️ Хранилище данных

### Normalized Store

```typescript
Store Structure:
{
  movies: {
    byId: {
      [movieId]: Movie
    }
  },
  
  episodes: {
    byMovieId: {
      [movieId]: Episode[]
    }
  },
  
  dashboard: {
    [tag]: movieId[]        // "My List": [1, 2, 3]
  },
  
  search: {
    results: movieId[]
  }
}
```

### Data Flow

```
Main Page (Component)
    ↓ FETCH
API: getDashboard()
    ↓ RESPONSE
Normalize & Store
    ↓
Redux Store (normalized)
    ↓ ACCESS by tag
PageContent → MovieList → MovieCard
    ↓ Click "More Info"
    ↓ FETCH (lazy)
API: getMovieDetails(id)
    ↓
Update Store → Render Full Card
```

**Ключевое:** Одна точка загрузки (PageContent), остальное — из store.

---

## 7. ⚡ Оптимизация

### A. Network Performance

#### 1. Asset Compression
```
✅ Gzip (30-50% экономии)
✅ Brotli (10-20% лучше Gzip)
```

#### 2. HTTP/2 — КРИТИЧНО! ⭐
```
Почему важен для Netflix:
- Multiplexing → много параллельных запросов
- Стриминг множества preview/видео
- Без HTTP/2: ограничение в 5-6 соединений
```

#### 3. Modern Image Formats
```
WebP → fallback PNG
AVIF (если поддерживается)
```

#### 4. GIFs for Promos ⭐
```
Проблема: Видео-превью = большой трафик
Решение: GIF анимации для hover-превью
Экономия: 80-90% трафика
```

#### 5. Image/Video Optimization Service ⭐

```
Client Request
     ↓
┌─────────────────────────────┐
│ Image/Video Optimizer       │
│ Input:                      │
│  - URL                      │
│  - Viewport (375px)         │
│  - Network (3G/4G/5G)       │
│ Output:                     │
│  - Optimized version        │
│  - Cached in CDN            │
└─────────────────────────────┘
```

**Примеры:**
- Mobile 3G: 480p, низкое качество
- Desktop WiFi: 1080p, высокое качество
- Tablet 4G: 720p, среднее качество

#### 6. Bundle Splitting

```javascript
Bundles:
├── vendors.js      // React, Redux (кэшируется долго)
├── player.js       // Lazy load при открытии видео
├── profile.js      // Lazy load
├── signup.js       // Отдельный bundle
└── dashboard.js    // Основной функционал
```

#### 7. Lazy Loading
```
✅ Movie details (cast, description) — при клике
✅ Player bundle — при воспроизведении
✅ Search results — при вводе
```

---

### B. Rendering Performance

#### 1. SSR для Sign Up ⭐

```html
Зачем:
- Критична конверсия пользователей
- Time to First Contentful Paint
- Time to Interactive

Решение:
- Server-Side Rendering
- Только HTML (без JS вначале)
- Мгновенная загрузка
```

#### 2. Critical CSS/JS Inline

```html
<head>
  <style>
    /* Critical CSS для above-the-fold */
    .header { ... }
    .promo-movie { ... }
  </style>
</head>
```

#### 3. Non-Critical Later
```
Загружаем позже:
- Analytics
- Дополнительные стили
- Редко используемые скрипты
```

#### 4. Preload для других страниц

```html
<link rel="preload" href="/profile.js" as="script">
```

#### 5. CSS Naming Strategy (BEM)

```css
/* ❌ Плохо */
.dashboard .list .card .image { }

/* ✅ Хорошо */
.movie-card__image { }
```

#### 6. Placeholders (Skeletons)

```
Loading state:
[████████] ← Skeleton
[████    ]
[██      ]

Психология: Кажется, что грузится быстрее!
```

#### 7. Animations на Composite Layer

```css
/* ✅ Используем transform (GPU) */
.movie-card:hover {
  transform: scale(1.05);
}

/* ❌ НЕ используем top/left (reflow) */
.movie-card:hover {
  top: -10px; /* МЕДЛЕННО! */
}
```

---

### C. JavaScript Performance

#### 1. Do Less Stuff
```typescript
// Избегаем синхронных тяжёлых операций
// Используем async/await, requestIdleCallback
```

#### 2. Web Workers
```typescript
// Тяжёлые вычисления в фоне
worker.postMessage({ type: 'PROCESS_VIDEO_METADATA' });
```

#### 3. Code Minification
```
Webpack + Terser
Удаление console.log в production
Tree shaking
```

---

### D. Caching

#### 1. Service Workers

```typescript
Cache Strategy:
- Static assets: Cache First
- API responses: Stale While Revalidate
- Videos: Network First (streaming)
```

#### 2. Browser Caching

```http
Cache-Control: max-age=31536000  # 1 год
ETag: "abc123"
```

---

### E. Smart TV Optimization ⭐

```javascript
Bundle для Smart TV (Chromium):
- ES6 modules (без полифиллов)
- Нативные API
- Минимальный размер
```

---

## 8. ♿ Доступность

### A. Subtitles (Субтитры) ⭐

```
Обязательны:
✅ Для глухих/слабослышащих
✅ Screen reader friendly
✅ Множество языков
```

### B. Color Schemas

```css
/* Темная тема (по умолчанию для Netflix) */
:root {
  --bg: #000;
  --text: #fff;
}

/* Адаптация для цветовой слепоты */
.color-blind-mode {
  --success: #0066cc;  /* Синий */
  --error: #ff9900;    /* Оранжевый */
}
```

### C. Keyboard Navigation (Hotkeys)

```
Горячие клавиши:
- Arrow Keys: Навигация по карточкам
- Enter: Открыть фильм
- Space: Play/Pause
- /: Поиск
- Esc: Закрыть модальное окно
- ?: Помощь (список hotkeys)
```

### D. ARIA Attributes

```html
<article 
  role="article"
  aria-label="Movie: Stranger Things"
>
  <button 
    aria-label="Play Stranger Things"
    aria-pressed="false"
  >
    ▶ Play
  </button>
</article>
```

### E. REM вместо Pixels

```css
/* Адаптация к browser zoom */
.movie-title {
  font-size: 1.5rem;  /* Масштабируется */
}
```

---

## 📊 Итоговая архитектура

```
┌─────────────────────────────────────────────┐
│              USER DEVICE                    │
│  (Smart TV / Mobile / Desktop)              │
├─────────────────────────────────────────────┤
│                                             │
│  ┌──────────┐      ┌───────────────┐       │
│  │  React   │◄─────│ Redux Store   │       │
│  │  UI      │      │ (Normalized)  │       │
│  └──────────┘      └───────────────┘       │
│       ▲                    ▲                │
│       │                    │                │
│  ┌────┴────────────────────┴─────┐         │
│  │    Service Worker (Cache)     │         │
│  └───────────────────────────────┘         │
│       ▲                    ▲                │
└───────┼────────────────────┼────────────────┘
        │                    │
        │                    │
┌───────┼────────────────────┼────────────────┐
│       │       CDN          │    Backend     │
│  ┌────┴────────┐    ┌──────┴─────┐         │
│  │ Image/Video │    │ API Server │         │
│  │ Optimizer   │    │            │         │
│  │ + Cache     │    │ /dashboard │         │
│  └─────────────┘    │ /search    │         │
│                     │ /movie/:id │         │
│                     └────────────┘         │
└─────────────────────────────────────────────┘
```

---

## 🎓 Ключевые выводы (Product Design)

### 1. **Холистический подход**
- Не углубляемся в детали
- Охватываем все основные фичи
- High-level архитектура

### 2. **Design System обязателен**
- Большое приложение = много команд
- Единая система компонентов
- Консистентность UI/UX

### 3. **Оптимизация стриминга критична**
- HTTP/2 для multiplexing
- GIF вместо видео для превью
- Адаптация к network bandwidth
- Image/Video Optimization Service

### 4. **SSR для конверсии**
- Sign Up page = критична
- Мгновенная загрузка
- Time to Interactive

### 5. **Мультиплатформенность**
- Smart TV (особые бандлы)
- Mobile (адаптация к экрану)
- Desktop (полный функционал)

---

## 🎯 На интервью фокусируйтесь на:

**Product Design требует:**
- ✅ Обоснование выбора фич
- ✅ Высокоуровневую архитектуру
- ✅ Понимание бизнес-приоритетов
- ✅ Trade-offs и компромиссы

**Типичные вопросы:**
- "Какие фичи самые важные для Netflix?"
- "Как оптимизировать для Smart TV?"
- "Как адаптировать под слабый интернет?"
- "Почему SSR для Sign Up?"

**Время:** ~45-60 минут на полный обзор

---

**Уровень:** Senior Frontend Engineer  
**Компании:** Netflix, Hulu, Disney+, Amazon Prime Video

Удачи на интервью! 🎬🍿