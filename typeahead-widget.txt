# Frontend System Design: Chat Application

## 📋 Введение

Добро пожаловать на урок по системному дизайну чат-приложения для фронтенд-инженеров. Рассмотрим архитектуру real-time мессенджера с фокусом на производительность и низкую latency.

---

## 📝 План системного дизайна

1. ✅ **Общие требования (General Requirements)**
2. ✅ **Функциональные требования (Functional Requirements)**
3. ✅ **Архитектура компонентов (Components Architecture)**
4. ✅ **API и протокол (Data API & Protocol)** ⭐ Критично!
5. ✅ **Модели данных (Data Entities)**
6. ✅ **Хранилище данных (Data Store)**
7. ✅ **Оптимизация (Optimization)**
8. ✅ **Доступность (Accessibility)**

---

## 1. 📌 Общие требования

### Основной функционал:

```
✅ Send messages (отправка сообщений)
✅ Receive messages (получение сообщений)
✅ Attach media (видео, аудио, фото, локация)
✅ Contact list (список контактов)
✅ Share messages (пересылка сообщений)
```

---

## 2. 🎯 Функциональные требования

### Ключевые требования:

**1. Low Latency (Низкая задержка) ⭐**
```
Требование: Real-time коммуникация
Допустимая задержка: 40-60ms
Критично для UX!
```

**2. Работа при плохом интернете**
```
Проблема: Мобильные сети нестабильны
Решение: Оптимизация для слабого канала
```

**3. Экономия ресурсов устройства**
```
Focus on:
- Минимальный трафик
- Сохранение батареи
- Низкое потребление памяти
```

**4. Поддержка устройств**
```
✅ Mobile (iOS, Android)
✅ Desktop (Web, Electron)
✅ Tablets
```

---

## 3. 🏗️ Архитектура компонентов

### Mockup структуры:

```
┌─────────────────────────────────────────┐
│         CHAT APPLICATION                │
├──────────────┬──────────────────────────┤
│ Contact List │  Chat View               │
│              │                          │
│ [Search]     │  ┌────────────────────┐ │
│              │  │ Messages           │ │
│ ┌──────────┐ │  │ ├─ Message 1      │ │
│ │ Avatar   │ │  │ ├─ Message 2      │ │
│ │ Contact1 │ │  │ └─ Attachments    │ │
│ └──────────┘ │  └────────────────────┘ │
│ ┌──────────┐ │                          │
│ │ Avatar   │ │  ┌────────────────────┐ │
│ │ Contact2 │ │  │ [Text Input]       │ │
│ └──────────┘ │  │ [Emoji] [Attach]   │ │
│              │  └────────────────────┘ │
└──────────────┴──────────────────────────┘
```

### Attachments (Вложения):

```
Types:
├── Video
├── Photo
├── Link
├── Contact
├── Location
└── Audio
```

### Message Actions:

```
Actions on message:
├── Reply (ответить)
├── Copy (копировать)
├── Share (переслать)
└── Delete (удалить)
```

### Component Hierarchy:

```
ChatApp
├── ContactList
│   ├── Search
│   └── Contact × N
│       ├── Avatar
│       └── Name
└── ChatView
    ├── MessageList
    │   └── Message × N
    │       ├── Content
    │       ├── Attachments
    │       └── MessageActions
    └── Controls
        ├── TextInput
        └── MediaDropdown
            ├── Attach Photo
            ├── Attach Video
            ├── Attach Location
            ├── Attach Contact
            ├── Emoji
            └── Record Audio
```

---

## 4. 🔌 API и протокол — КРИТИЧНО! ⭐

### Выбор протокола: Сравнение

#### A. Long Polling

**Pros:**
```
✅ HTTP benefits (кэширование, стандартность)
✅ Очень простая реализация
✅ Работает везде
```

**Cons:**
```
❌ Высокая latency (особенно на мобильных)
❌ Переключение network cells → потеря соединения
❌ Connection timeouts (из-за proxy)
❌ Traffic overhead (headers, tokens при каждом запросе)
```

**Вердикт:** ❌ НЕ подходит для real-time чата

---

#### B. WebSockets

**Pros:**
```
✅ Duplex communication (двусторонняя)
✅ Минимальная latency (~10ms)
✅ Real-time коммуникация
```

**Cons:**
```
❌ Keeping connection expensive (ресурсы!)
❌ 10 tabs = 10 connections → батарея разряжается
❌ НЕ совместим с HTTP/2
❌ Сложная балансировка нагрузки
❌ Нужно реализовывать сжатие, multiplexing вручную
❌ Проблемы с firewalls/proxies (TCP фильтрация)
```

**Вердикт:** ❌ Создаёт больше проблем, чем решает

---

#### C. Server-Sent Events (SSE) ⭐ ВЫБОР!

**Pros:**
```
✅ Все преимущества HTTP/2
✅ Совместим с HTTP/2 из коробки
✅ НЕТ traffic overhead (только данные, без headers)
✅ Resource efficient (не тратит батарею)
✅ Простая балансировка нагрузки
✅ Auto-reconnect
✅ Text format (легко парсить)
```

**Cons:**
```
❌ Weird API (странный интерфейс)
❌ Unidirectional (только получение, не отправка)
❌ Only text data (нужно парсить)
```

---

### 🏆 Выбор: Hybrid Approach (SSE + HTTP)

```
┌─────────────────────────────────────────┐
│            CLIENT                       │
├─────────────────────────────────────────┤
│                                         │
│  Receive Messages:                      │
│  ◄────── SSE (Server-Sent Events)       │
│          - Low latency                  │
│          - HTTP/2 benefits              │
│          - No overhead                  │
│                                         │
│  Send Messages:                         │
│  ──────► HTTP POST                      │
│          - Simple                       │
│          - Rare operation               │
│          - Traffic overhead OK          │
│                                         │
└─────────────────────────────────────────┘
         ▲              │
         │              ▼
    ┌────────────────────────┐
    │      API SERVER        │
    └────────────────────────┘
```

**Обоснование:**
- Получаем сообщений БОЛЬШЕ, чем отправляем
- SSE для получения = оптимально
- HTTP POST для отправки = редкая операция, overhead приемлем
- Trade-off оправдан!

---

## 5. 📊 Модели данных

```typescript
interface Contact {
  id: string;
  name: string;
  avatar?: string;
  lastMessage?: string;
  unreadCount?: number;
}

interface Message {
  id: string;
  content: string;
  attachments?: Attachment[];  // Опционально
  authorId: string;            // Отправитель
  receiverId: string;          // Получатель
  timestamp: number;
  status: 'sent' | 'delivered' | 'read';
}

interface Attachment {
  id: string;
  messageId: string;
  type: 'link' | 'video' | 'audio' | 'image' | 'location' | 'contact';
  content: string;             // URL ресурса
}
```

---

## 6. 🔌 API Endpoints

```typescript
// 1. Subscribe к новым сообщениям (SSE)
SSE /api/messages/subscribe
Params:
  - apiKey: string
  - userId: string
  - timestamp: number      // Точка синхронизации
Response: Stream of Message[]

// 2. Получить контакты
GET /api/contacts
Params:
  - apiKey: string
  - userId: string
Response: Contact[]

// 3. Отправить сообщение (HTTP POST)
POST /api/messages/send
Body:
  - apiKey: string
  - userId: string
  - message: Message

// 4. Загрузить медиа
POST /api/media/attach
Body:
  - apiKey: string
  - userId: string
  - messageId: string
  - data: Binary
```

---

## 7. 🗄️ Хранилище данных

### Normalized Store

```typescript
Store Structure:
{
  contacts: {
    byId: {
      [contactId]: Contact
    }
  },
  
  messages: {
    byContactId: {
      [contactId]: Message[]
    }
  },
  
  attachments: {
    byContactId: {
      [contactId]: Attachment[]
    }
  }
}
```

### Data Flow

```
ChatApp (Component)
    ↓ FETCH
API: getContacts()
    ↓
Store (normalized)
    ↓ Click contact
ChatView
    ↓ Subscribe SSE
API: /messages/subscribe?timestamp=...
    ↓ Stream
Store: messages.byContactId[contactId]
    ↓ Render
MessageList → Message × N
```

**Ключевое:**
- Subscribe на `timestamp` = точка синхронизации
- Получаем только новые сообщения после этой точки
- Нормализованное хранилище для быстрого доступа

---

## 8. ⚡ Оптимизация

### A. Network Performance

**1. Compression**
```
✅ Gzip (50% экономии)
✅ Brotli (10-20% лучше)
```

**2. HTTP/2 ⭐**
```
Критично:
- Multiplexing
- Параллельная загрузка ресурсов
- Совместимость с SSE
```

**3. Bundle Splitting**
```javascript
Bundles:
├── vendors.js      // React, Redux
└── chat.js         // Основной функционал
```

**4. ES6 Bundle для современных устройств**
```
Без полифиллов → меньше кода
```

**5. Modern Image Formats**
```
WebP → fallback PNG
Экономия трафика!
```

**6. Image Optimization Service**
```
Mobile → 400px width
Desktop → 800px width
+ CDN caching
```

**7. Efficient Caching**
```http
ETag, Last-Modified
Browser + CDN cache
```

**8. Minify Resources**
```
JS, CSS minification
```

**9. Non-critical resources later**
```html
<link rel="preconnect"> для некритичных ресурсов
```

---

### B. Rendering Performance

**1. Inline Critical CSS/JS**
```html
<style>/* Critical CSS */</style>
```

**2. Load Non-critical Later**
```
Analytics, дополнительные скрипты
```

**3. CSS Naming Strategy (BEM)**
```css
/* ❌ Плохо */
.chat .message .text { }

/* ✅ Хорошо */
.message__text { }
```

**4. Avoid Reflows**
```css
/* ✅ Используем transform (GPU) */
.message:hover {
  transform: translateY(-2px);
}
```

**5. Placeholders (Skeletons)**
```
Показываем skeleton во время загрузки
→ Ощущение быстрой загрузки
```

**6. Infinite Scroll с константным числом DOM-нод**
```
Проблема: 1000 сообщений = 1000 DOM элементов
Решение: Всегда 10 нод, переиспользуем и обновляем данные
```

---

### C. JavaScript Performance

**1. Do Less Stuff**
```
Минимум вычислений
Асинхронность где возможно
```

**2. Web Workers**
```
Если нужны тяжёлые вычисления
(но для чата обычно не требуется)
```

**3. Minify Code**
```
Меньше полифиллов = лучше производительность
```

**4. Service Workers**
```
Cache:
- Static assets
- CSS
- Message data
- Images
→ Offline режим!
```

---

## 9. ♿ Доступность

### A. Use REMs (не Pixels)

```css
/* Адаптация к browser zoom */
.message {
  font-size: 1rem;  /* Масштабируется */
  padding: 0.5rem;
}
```

---

### B. Keyboard Shortcuts

```
Hotkeys:
├── Next/Previous contact (↑/↓)
├── Ctrl+C: Proceed to message
├── Ctrl+A: Open attachment menu
├── Ctrl+F: Search for contact
├── Ctrl+S: Search for message
└── ?: Open help menu
```

---

### C. Color Schemas

```css
/* Адаптация для цветовой слепоты */
.color-blind-mode {
  --success: #0066cc;  /* Синий вместо зелёного */
  --error: #ff9900;    /* Оранжевый вместо красного */
}
```

---

### D. ARIA Attributes

```html
<div 
  role="log"
  aria-live="polite"
  aria-label="Chat messages"
>
  <!-- Скринридер озвучивает новые сообщения -->
</div>

<img 
  src="photo.jpg" 
  alt="Photo from John"
/>
```

---

### E. Semantic HTML

```html
<!-- Правильная семантика -->
<main>
  <section aria-label="Contact list">
    <article>...</article>
  </section>
</main>
```

---

## 📊 Итоговая архитектура

```
┌─────────────────────────────────────────┐
│            CLIENT                       │
│  ┌──────────┐      ┌────────────┐      │
│  │ React UI │◄─────│Redux Store │      │
│  └──────────┘      └────────────┘      │
│       ▲                   ▲             │
│       │                   │             │
│  ┌────┴───────────────────┴────┐       │
│  │   Service Worker (Cache)    │       │
│  └─────────────────────────────┘       │
│       ▲                   ▲             │
└───────┼───────────────────┼─────────────┘
        │                   │
        │ SSE Subscribe     │ HTTP POST
        │ (Receive)         │ (Send)
        │                   │
┌───────┼───────────────────┼─────────────┐
│       ▼                   ▼             │
│  ┌─────────────────────────────┐       │
│  │      API SERVER             │       │
│  │  - /messages/subscribe      │       │
│  │  - /messages/send           │       │
│  │  - /contacts                │       │
│  └─────────────────────────────┘       │
│                                         │
│  ┌─────────────────────────────┐       │
│  │  Image Optimization + CDN   │       │
│  └─────────────────────────────┘       │
└─────────────────────────────────────────┘
```

---

## 🎓 Ключевые выводы

### 1. **Hybrid Approach — оптимальное решение**
```
SSE для получения (частая операция)
+ HTTP для отправки (редкая операция)
= Баланс производительности и простоты
```

### 2. **Low Latency критична**
```
Требование: 40-60ms
Решение: SSE + HTTP/2
```

### 3. **Экономия ресурсов**
```
- Минимальный трафик (SSE без overhead)
- Сохранение батареи (no constant TCP)
- Эффективное кэширование
```

### 4. **Оптимизация для мобильных**
```
- WebP images
- Минимальные bundle sizes
- Offline mode (Service Workers)
```

### 5. **Real-time без WebSockets**
```
SSE + HTTP/2 = лучше чем WebSockets
для большинства chat use cases
```

---

## 🎯 Trade-offs обоснование

**Почему НЕ WebSockets:**
```
❌ Высокое потребление ресурсов
❌ Сложная инфраструктура
❌ Проблемы с HTTP/2
❌ Батарея разряжается быстрее
```

**Почему SSE + HTTP:**
```
✅ HTTP/2 совместимость
✅ Простая балансировка
✅ Низкое потребление ресурсов
✅ Нет traffic overhead
✅ Получаем >> отправляем (trade-off оправдан)
```

---

**Время выполнения:** ~45 минут  
**Уровень:** Middle/Senior Frontend Engineer  
**Компании:** WhatsApp, Telegram, Slack, Discord

Удачи на интервью! 💬🚀